public class CorrectTypesConversion {
    (Integer.parseInt(String.valueOf(num)) + Integer.parseInt(String.valueOf(village.charAt(s))) == 10) ->
    boolean sleepwalkerDetected = (beginningCitizenValue + endingCitizenValue == TEMPERATURE_DECREASE);

    total % 2 == 0 -> if(isTotalEven);

    temp != counter && temp != 0 -> if(!isPassword);

    file.isFile() && file.getName().contains(ext) -> isCorrectFile

    if(doors[j] == 0) -> isClosedDoor

    tree[i].charAt(j) == '+' -> isBranch

    sym1 <= sym2 -> isGreaterElement;

    (input.charAt(j + 1) - dec_element) <= dif && (input.charAt(j + 1) - dec_element) > 0 ->
        isShorterLexicographicWord;

     if(Arranger(arr)) -> if(isSortableInOneStep);

     if(price < min_price) -> isCheaper;

     if(last_digit != digit_to_remove) - > !isDigitToRemove;

     if(grade % 3 == 0) -> isGradeThree;

}
